
## 一、Web安全概述

### 1.1 什么是Web安全

Web安全是包括网站、网络应用、网络服务在内的一系列安全内容，关注的是应用层的安全，尤其是细化到与网络相关内容的安全。Web安全主要关注保护Web应用程序的安全性，防止未经授权的访问、数据泄露、篡改等安全威胁。

Web安全需要学习的内容主要包括网络协议本身、网络应用的特性与对应的安全问题、各种工具的使用。

### 1.2 Web发展简史

了解Web的发展历程，有助于理解安全问题的演变。

| 阶段 | 时间 | 核心特征 | 主要漏洞类型 |
|------|------|----------|--------------|
| **Web 1.0** | 1990年代末~2000年代初 | 静态内容为主，用户单向获取信息 | 钓鱼攻击、URL跳转、框架漏洞、XSS、CSRF |
| **Web 2.0** | 2000年代初至今 | 用户参与生成内容，社交互动 | SQL注入、文件包含、命令执行、上传漏洞、WebShell |
| **Web 3.0** | 未来发展趋势 | 语义化数据、AI驱动、区块链技术 | 去中心化应用安全、智能合约安全 |

### 1.3 Web相关概念区分

- **互联网**：全球范围的计算机网络
- **因特网**：以TCP/IP协议连接全球的计算机网络
- **万维网（World Wide Web）** ：互联网上的一种信息服务，通过浏览器访问

三者关系：**互联网 > 因特网 > 万维网**


## 二、Web核心基础知识

### 2.1 HTTP/HTTPS协议

HTTP是Web通信的基础协议，HTTPS在其基础上增加了SSL/TLS加密。

**URL结构**：`协议://域名:端口/路径?参数#锚点`

**HTTP请求方法**：
- GET：获取资源
- POST：提交数据
- PUT：上传资源
- DELETE：删除资源

**HTTP状态码**：
- 2xx：成功（如200 OK）
- 3xx：重定向（如302 Found）
- 4xx：客户端错误（如403 Forbidden、404 Not Found）
- 5xx：服务器错误（如500 Internal Server Error）

**HTTP报文**：包含请求行/状态行、请求头/响应头、空行、消息体

### 2.2 会话管理

- **Cookie机制**：服务器发送给客户端的小型文本数据，用于维持会话状态
- **Session机制**：服务端存储的会话数据，通过Session ID关联

### 2.3 同源策略

同源策略是浏览器最核心的安全机制之一。它限制了一个源的文档或脚本如何与另一个源的资源进行交互。同源要求协议、域名、端口完全相同。

### 2.4 前端基础

- **HTML**：页面结构，包含表单、链接等交互元素
- **CSS**：页面样式
- **JavaScript**：页面交互逻辑，受同源策略约束

### 2.5 后端基础

- 数据库基本操作（SQL）
- 服务端框架路由原理
- API交互模式

### 2.6 编码基础

常见的编码类型包括：ASCII编码、UTF-8编码、URL编码、Unicode编码、GBK编码、Base64编码等。


## 三、常见Web安全漏洞

### 3.1 OWASP Top 10

OWASP Top 10是Web安全领域最具权威性的漏洞列表，核心漏洞类型包括：
- 注入攻击（SQL注入、命令注入）
- 跨站脚本（XSS）
- 失效的访问控制
- 安全配置错误
- 逻辑漏洞

### 3.2 SQL注入

**原理**：攻击者通过在用户输入中插入恶意SQL代码，欺骗后端数据库执行非预期操作。

**常见类型**：
- 联合查询注入
- 布尔盲注
- 时间盲注

**防御措施**：使用参数化查询、输入验证与过滤、最小权限原则。

### 3.3 跨站脚本攻击（XSS）

**原理**：攻击者将恶意脚本注入到网页中，当用户访问时在浏览器端执行。

**常见类型**：
- **反射型XSS**：恶意脚本通过URL参数反射回页面
- **存储型XSS**：恶意脚本存储在服务器端
- **DOM型XSS**：通过修改DOM环境执行恶意脚本

**危害**：窃取Cookie、DOM污染、构造钓鱼页面

### 3.4 CSRF（跨站请求伪造）

**原理**：攻击者通过伪装来自受信任用户的请求，改变服务端数据。

**攻击条件**：重要操作的所有参数都可以被攻击者猜解到。

### 3.5 文件上传漏洞

**原理**：网站对上传文件校验不严，攻击者可上传恶意文件（如WebShell）。

**绕过方式**：
- 前端校验绕过
- 黑名单/白名单绕过
- Content-Type绕过
- 文件头校验绕过

### 3.6 其他常见漏洞

- **SSRF（服务端请求伪造）** ：攻击者利用服务端发起内网请求
- **文件包含漏洞**：包含非预期的文件
- **命令执行（RCE）** ：远程执行系统命令
- **反序列化漏洞**：恶意序列化数据导致代码执行
- **XXE（XML外部实体注入）** ：利用XML解析器读取本地文件


## 四、Web安全实践环境

### 4.1 常用工具

| 工具 | 用途 | 说明 |
|------|------|------|
| **Burp Suite** | Web安全测试集成工具 | 拦截代理、爆破、重放等功能 |
| **SQLMap** | SQL注入自动化检测 |  |
| **Nmap** | 端口扫描与信息收集 |  |
| **浏览器开发者工具** | 抓包、调试 | Elements/Console/Network面板 |

### 4.2 靶场平台

靶场是练习Web安全漏洞挖掘的实战环境。

| 靶场 | 特点 | 适用阶段 |
|------|------|----------|
| **DVWA** | 新手必练，包含暴力破解、命令注入、CSRF、文件包含、SQL注入、XSS等 | 入门 |
| **sqli-labs** | SQL注入专项闯关 | SQL注入专项 |
| **upload-labs** | 文件上传专项 | 文件上传专项 |
| **WebGoat** | OWASP出品，30+训练课程 | 进阶 |
| **Pikachu** | 综合性靶场 | 综合练习 |
| **PortSwigger Academy** | 200+在线靶场，覆盖OWASP Top 10 | 系统学习 |

### 4.3 环境搭建

推荐使用 **PHPStudy** 集成环境快速部署Web安全学习环境。也可使用 **VMware** 搭建Win10+Kali双虚拟机环境。




