# 一、网络安全方向初始

# **二、** Linux常用命令
| 命令 | 作用 | 常用选项 |
|---|---|---|
| `pwd` | 显示当前路径 | - |
| `ls` | 列出目录 | `-l` `-a` `-h` |
| `cd` | 切换目录 | `..` `~` `-` |
| `mkdir` | 创建目录 | `-p` |
| `touch` | 创建文件 | - |
| `echo` | 输出文本 | `>` `>>` |
| `cat` | 查看文件 | `-n` |
| `cp` | 复制 | `-r` `-i` |
| `mv` | 移动/重命名 | `-i` `-u` |
| `rm` | 删除 | `-r` `-f` `-i` |
| `grep` | 搜索文本 | `-i` `-r` `-n` |
| `find` | 查找文件 | `-name` `-size` `-type` |
| `chmod` | 修改权限 | `+x` `-R` `755` |
| `ps` | 查看进程 | `aux` `-ef` |
| `ip` | 网络配置 | `addr` `link` `route` |
| `curl` | 网络请求 | `-O` `-I` `-L` |
| `man` | 帮助手册 | 按`q`退出 |
| `sudo` | 提权执行 | `-i` `-u` |
| `exit` | 退出 | - |
# 三、相关安全工具的使用场景

## 1. Burp Suite
**主要场景**：Web应用安全测试（渗透测试）
- 拦截、查看、修改浏览器与服务器之间的HTTP/HTTPS请求和响应
- 用于发现SQL注入、XSS、CSRF等常见Web漏洞
- 可自动化扫描Web漏洞（专业版功能）
- 支持重放请求、登录爆破、会话令牌分析等操作

## 2. Wireshark
**主要场景**：网络流量分析与抓包
- 实时捕获网络数据包（如TCP、UDP、HTTP、DNS等）
- 用于排查网络故障、分析协议交互、检测恶意流量
- 支持强大的过滤、搜索功能，可追踪TCP流
- 常用于网络安全分析、逆向工程、网络编程调试

## 3. 010 Editor
**主要场景**：二进制文件编辑与解析
- 以十六进制形式查看和编辑任何文件（如可执行文件、图片、磁盘镜像等）
- 内置模板脚本，可解析复杂的二进制结构（如PE、ELF、PDF、ZIP等格式）
- 用于逆向工程、文件格式分析、恶意软件拆解、数据恢复

## 4. gdb (GNU Debugger)
**主要场景**：程序调试与逆向分析（主要针对C/C++）
- 调试运行中的程序（设置断点、单步执行、查看内存/寄存器/调用栈）
- 分析程序崩溃原因（core dump文件）
- 常用于漏洞挖掘（如缓冲区溢出）、反汇编、修改程序执行流
- 常结合pwntools等工具用于CTF解题或漏洞利用开发

## 5. Python3
**主要场景**：通用编程及安全领域自动化
- 编写渗透测试脚本（如端口扫描、爆破、漏洞验证、自动化攻击）
- 进行数据解析与处理（如抓包后的流量分析、日志处理）
- 开发各类安全工具（如爬虫、模糊测试器、加密解密工具）
- 用于CTF中的编码转换、算法逆向、PWN题利用脚本编写
